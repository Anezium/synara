# Codex vs Synara computer use parity matrix

## Scope

This doc maps each Codex computer use capability to the Synara equivalent on branch `agent/computer-use-preview` at native revision 15 and driver 0.28.2. Codex claims come from third party reconstructions and local binary scans in the mega handoff, so each is marked unverified. Ours claims come from the tool surface, the backend, the desktop host, and repo docs. Runtime behavior at revision 15 is uncertified until a signed app run with fresh permissions passes. The Chrome path is deferred by decision. Menus and window frames are in milestone one by decision.

## Capability matrix

| Capability               | Codex                                                                                                                       | Ours                                                                                                                                                                             | Gap      | Target   | Evidence                                                                                                                     | Notes                                                                                                                         |
| ------------------------ | --------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | -------- | ---------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| click                    | Per process synthetic click with field stamping, AXPress first, masked real activation for Electron class apps (unverified) | `computer_click` plus double, triple, right variants on an exact window target, background by default, one transport per event                                                   | Partial  | P1       | `apps/server/src/agentGateway/computerTools.ts:2296`, `docs/computer-use-cua/README.md:62`                                   | Masked activation and synthetic focus belief are missing. Never replay uncertain clicks.                                      |
| drag                     | Same event stream with drag phases in background (unverified)                                                               | Foreground only on macOS, 10 second cap, both endpoints inside the exact window                                                                                                  | Partial  | P1       | `apps/server/src/computer/CuaComputerBackend.ts:1146`, `docs/computer-use-cua/README.md:65`                                  | Background drag is the gap. Stop during drag is fixture covered (unverified at rev 15).                                       |
| scroll                   | Wheel deltas plus AX scrollbar fallback (unverified)                                                                        | One axis, no modifiers, 120 pixel notches, max 50, no travel measurement on macOS, half frame clamp, repeat unchanged refusal                                                    | Partial  | P1       | `apps/server/src/computer/CuaComputerBackend.ts:1227`, `apps/server/src/computer/ComputerManager.ts:1726`                    | Scroll pain is user reported. AX scroll actions and 2 axis support are open.                                                  |
| type_text                | Layout aware keys plus layout free literal path (unverified)                                                                | Focus neutral AX insert when one writable target is proven, else synthetic keyboard on the active Space                                                                          | Partial  | P1       | `apps/server/src/agentGateway/computerTools.ts:2497`, `docs/computer-use-cua/evidence/rev15-electron-2026-09-17-notes.md:30` | Three window concurrent typing failed at rev 15 with effect dispatched-unknown (proven by fixture). Isolation is uncertified. |
| press_key                | Xdotool style keysym table (unverified)                                                                                     | One key plus hotkey chords, single non modifier key on macOS, no Insert mapping                                                                                                  | Partial  | P2       | `apps/server/src/agentGateway/computerTools.ts:2520`, `apps/server/src/computer/CuaComputerBackend.ts:112`                   | Near parity. Key coverage deltas are unverified.                                                                              |
| set_value                | AX value write with search autosubmit (unverified)                                                                          | Atomic element token write, no synthetic fallback, passed the rev 15 electron run                                                                                                | Parity   | Done     | `apps/server/src/agentGateway/computerTools.ts:2621`, `apps/server/src/computer/CuaComputerBackend.ts:1298`                  | Keep the no fallback rule. Autosubmit behavior is unverified.                                                                 |
| perform_secondary_action | Named AX action on an indexed element (unverified)                                                                          | AXPress only, all other names refused                                                                                                                                            | Partial  | P1       | `apps/server/src/agentGateway/computerTools.ts:2644`, `apps/server/src/computer/CuaComputerBackend.ts:1301`                  | Menu actions ride on this gap. See invoke_menu decision below.                                                                |
| paste                    | Pasteboard write plus clipboard restore (unverified)                                                                        | Save, write, paste, restore with a clipboardRestored report                                                                                                                      | Parity   | Done     | `apps/server/src/agentGateway/computerTools.ts:2579`, `docs/computer-use-cua/README.md:69`                                   | Non text clipboard contents cannot be saved and are replaced.                                                                 |
| select_text              | Text marker selection (unverified)                                                                                          | No select_text tool, triple click or app select all used instead                                                                                                                 | Missing  | P2       | `apps/server/src/agentGateway/computerTools.ts:2499`, `docs/computer-use-cua/capability-audit-2026-09-16.md:9`               | Absence is proven by the tool list. Priority is low since workarounds exist.                                                  |
| get_app_state/skyshot    | Window shot plus AX tree with element ids, diff mode, AX observer settle (unverified)                                       | `computer_get_state` with elements plus optional image and text and diff mode, fixed 300 ms settle                                                                               | Partial  | P1       | `apps/server/src/agentGateway/computerTools.ts:1940`, `apps/server/src/computer/ComputerManager.ts:111`                      | AX observer settle and tree shaping are missing.                                                                              |
| list_apps/list_windows   | App list plus window list (unverified)                                                                                      | `computer_list_windows` only, no app list, minimized and off Space windows can drop out without the capture grant                                                                | Partial  | P2       | `apps/server/src/agentGateway/computerTools.ts:1906`, `apps/server/src/agentGateway/computerTools.ts:2805`                   | Selection never activates. Completeness limits are documented.                                                                |
| launch_app               | Approved app launch (unverified)                                                                                            | Approval gated launch by name or bundle id, no absolute paths                                                                                                                    | Parity   | Done     | `apps/server/src/agentGateway/computerTools.ts:2266`, `apps/server/src/computer/CuaComputerBackend.ts:1331`                  | Near parity. URL policy checks from Codex are missing (unverified need).                                                      |
| activate_window          | Make window key with focus bookkeeping (unverified)                                                                         | Foreground only, approval gated, restores the prior frontmost window                                                                                                             | Partial  | P1       | `apps/server/src/agentGateway/computerTools.ts:2598`, `apps/server/src/computer/ComputerManager.ts:1508`                     | Synthetic key without raise is missing. No auto promotion from background refusals.                                           |
| screenshots              | Fast private path for previews plus ScreenCaptureKit model shots (unverified)                                               | Exact window shots plus primary display overview, no region args on macOS, off Space freshness refused                                                                           | Partial  | P1       | `apps/server/src/agentGateway/computerTools.ts:2060`, `apps/server/src/computer/CuaComputerBackend.ts:663`                   | Mixed scale and second display coverage are unqualified.                                                                      |
| wait/settle              | AX observer wait for settle with busy extension (unverified)                                                                | `computer_wait` plus fixed settle and bounded observation windows                                                                                                                | Partial  | P1       | `apps/server/src/agentGateway/computerTools.ts:2152`, `apps/server/src/computer/ComputerManager.ts:111`                      | Conditional settle on read back proof is open work.                                                                           |
| virtual cursor           | Spring cursor overlay, visual only, multi cursor (unverified)                                                               | Compact cursor overlay, glide 100 ms dwell 0, overlay only, no hover                                                                                                             | Partial  | P2       | `apps/desktop/src/cuaDriverHost.ts:719`, `docs/computer-use-cua/README.md:67`                                                | Cursor never aims input. Multi cursor for parallel agents is missing.                                                         |
| per-app approvals        | Allow, always allow, deny per app plus launch approvals and Escape stop (unverified)                                        | Per action approval rendezvous, 128 global cap, foreground always needs explicit approval, no always allow, no Escape stop                                                       | Partial  | P1       | `apps/server/src/computer/ComputerApprovalGate.ts:18`, `docs/computer-use-cua/README.md:52`                                  | Always allow is a safety tradeoff, not just a gap. Needs an explicit decision.                                                |
| Spaces handling          | Behavior unverified                                                                                                         | Active Space routing for pointer, keys, scroll, drag, activation. Retained semantic text may survive Space moves when identity holds. Off Space pixels are never live grounding. | Partial  | P1       | `apps/server/src/computer/CuaComputerBackend.ts:663`, `apps/server/src/computer/CuaComputerBackend.ts:886`                   | Certified behavior is missing at rev 15. Space churn must not wedge the run.                                                  |
| locked use               | Auth plugin with auto unlock during turns and relock on local input (unverified)                                            | Pause on lock, sleep, and session resign, resume after. No operation while locked.                                                                                               | Missing  | Later    | `apps/desktop/src/computerDesktopLifecycle.ts:27`                                                                            | Locked operation needs an explicit safety decision first.                                                                     |
| Chrome/CDP path          | CDP through an extension, not AX, with tab APIs and takeover checks (unverified)                                            | Deferred by decision. Browser work uses the separate browser surface. Computer use stays the native app fallback.                                                                | Deferred | Deferred | `docs/computer-use-cua/capability-audit-2026-09-16.md:18`, `docs/computer-use-cua/README.md:70`                              | Without the extension Codex also falls back to AX (unverified).                                                               |
| record/replay history    | Skysight history plus event recorder with AX diffs (unverified)                                                             | Raw session and recording tools are internal only. Preview lifecycle is owned by Synara. No agent facing history.                                                                | Missing  | Later    | `docs/computer-use-cua/capability-audit-2026-09-16.md:28`, `docs/computer-use-cua/capability-audit-2026-09-16.md:17`         | Optional for parity. Privacy policy must come before any recording surface.                                                   |
| invoke_menu (extra)      | AX first menu actioning (unverified)                                                                                        | Deliberately unexposed, needs ownership, approval, and effect semantics                                                                                                          | Missing  | P1       | `docs/computer-use-cua/capability-audit-2026-09-16.md:24`, `apps/server/src/computer/CuaComputerBackend.ts:1301`             | In milestone one by decision. Exposure must not bypass targeting.                                                             |
| set_window_frame (extra) | Window motion support (unverified)                                                                                          | Deliberately unexposed, needs bounds policy and visible approval                                                                                                                 | Missing  | P1       | `docs/computer-use-cua/capability-audit-2026-09-16.md:25`, `docs/computer-use-cua/README.md:70`                              | In milestone one by decision. Cross platform contract is open.                                                                |

## Gap summary

Ranked by user pain and milestone order. Codex side items stay unverified until measured against the shipping binary.

1. Type text isolation. The rev 15 fixture proves three window typing fails with dispatched unknown. This blocks the core promise. Target P1 with certification.
2. Synthetic focus belief. Click, Electron apps, and key windows depend on it. This unlocks parity for apps that demand active state. Target P1.
3. Scroll v2. Single axis quantized scroll with no measurement is the loudest usability gap. Target P1.
4. Menus and window frames. Both are deliberately unexposed and both are in milestone one by decision. Target P1.
5. Settle and observation. Fixed 300 ms settle plus no AX observer settle slows every turn and weakens read back. Target P1.
6. Background drag. Foreground only drag forces activation excursions. Target P1 after focus work.
7. Synthetic key activation. Raising a window just to aim keys steals focus. Target P1.
8. Screenshots and Spaces certification. Freshness refusals and routing exist in source but are uncertified at rev 15. Target P1.
9. Per app approvals and always allow. Needs a safety decision before any build. Target P1 for the decision, P2 for build.
10. Secondary actions beyond AXPress. Needed for menus and richer controls. Target P1.
11. Click recipe breadth. Native versus Chromium recipe selection exists but Electron masking is missing. Target P2.
12. Cursor richness and multi cursor. Visual only today. Target P2.
13. List apps, select_text, press_key coverage. Small gaps with workarounds. Target P2.
14. Locked use and history. Both need safety and privacy decisions first. Target Later.
15. Chrome CDP path. Deferred by decision. Revisit only after native parity lands.

## Status update — 2026-09-17 live experiments (this VM, full TCC)

The gap summary above predates the decisive input matrix. Verified results:

- **Gap 1 (type text isolation) — RESOLVED.** Web-content fields now route
  through verified `set_value` compose; three-window same-pid typing passes
  with `verified/confirmed`, serialized per-pid lane, zero focus steal.
- **Gap 2 (synthetic focus belief) — DEAD by experiment.** Six belief stages
  including a real `_SLPSSetFrontProcessWithOptions` front flip cannot
  manufacture a key window; process-scoped `CGEventPostToPid` (keys, moves,
  wheel) never reaches an inactive renderer. Belief work is cancelled.
- **Gap 6 (background drag) and 7 (synthetic key activation) —
  RECHARACTERIZED.** No background CGEvent path exists on inactive Electron.
  Real input events exist only through `delivery_mode=foreground`
  (short-lived front excursion + restore, operator-invisible): verified for
  keys, clicks (DOM focus + mouseenter), and wheel. Background = semantic AX
  writes only.
- **Gap 4 (menus/frames) — WIRED.** `invoke_menu`, `set_window_frame`,
  `list_apps`, `verify_state`, `zoom`, `kill_app` verified live on TextEdit
  and shipped through protocol/backend/manager/gateway as
  `computer_invoke_menu`, `computer_set_window_frame`, `computer_list_apps`,
  `computer_verify_state`, `computer_zoom`, `computer_kill_app`. Mutations
  are approval-gated with second-app consent and Synara-side read-back;
  `verify_state` returns a tri-state that cannot contradict the scoped
  observation contract.
- **Gap 3 (scroll v2) — CONFIRMED.** Background scroll returns
  `background_unavailable`; foreground scroll delivers real DOM wheel events.
  Options: keep scroll foreground-only, or implement rev-16 scroll with
  AX-first + 2-axis + modifiers per the subagent map.

## Driver tool inventory — definitive audit (0.28.2, native rev 16, embedded serve)

Every tool the managed macOS driver registers, classified once. Registration
comes from `platform-macos` `tools::register_all` plus the binary's host
tools (`check_for_update`, and `history_status`/`history_query` only under the
upstream preview admission — the embedded daemon Synara spawns does not admit
them, so 57 names exist on the wire here, 59 upstream). Names in the contract
vocabulary that macOS never registers are listed last.

The allowlist boundary is machine-pinned by
`packages/shared/src/cuaDriverProtocol.test.ts`: a driver tool not in
`CUA_READ_TOOLS`/`CUA_ACTION_TOOLS` is unreachable from the server side, and
growing the allowlist means re-auditing the tool here first.

### Agent-facing (`computer_*` tools)

| Driver tool              | Synara surface                                                                                                                                                                      |
| ------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `list_windows`           | `computer_list_windows`                                                                                                                                                             |
| `get_window_state`       | `computer_get_state` / `computer_screenshot` (exact-window tree + capture)                                                                                                          |
| `get_desktop_state`      | `computer_get_state` / `computer_screenshot` (whole-desktop overview)                                                                                                               |
| `get_screen_size`        | `computer_get_screen_size`                                                                                                                                                          |
| `list_apps`              | `computer_list_apps`                                                                                                                                                                |
| `get_accessibility_tree` | `computer_get_accessibility_tree` (desktop inventory; `window_id` scopes to the owning app)                                                                                         |
| `get_cursor_position`    | `computer_get_cursor_position`                                                                                                                                                      |
| `verify_state`           | `computer_verify_state` (tri-state preserved verbatim)                                                                                                                              |
| `zoom`                   | `computer_zoom` (display-only; never a coordinate frame)                                                                                                                            |
| `click`                  | `computer_click`; `computer_double_click`/`computer_triple_click` via `count`; `computer_right_click` via `button:"right"`; `computer_perform_action` via `element_token` (AXPress) |
| `move_cursor`            | `computer_move_cursor` (overlay-only move; never aims input)                                                                                                                        |
| `drag`                   | `computer_drag` (foreground delivery only)                                                                                                                                          |
| `scroll`                 | `computer_scroll` (rev-16 two-axis + modifiers + measured travel)                                                                                                                   |
| `type_text`              | `computer_type_text` (focus-neutral AX insert or synthetic keys)                                                                                                                    |
| `press_key`              | `computer_press_key`                                                                                                                                                                |
| `hotkey`                 | `computer_hotkey`                                                                                                                                                                   |
| `set_value`              | `computer_set_value` (exact element-token write, no synthetic fallback)                                                                                                             |
| `clipboard_read`         | `computer_read_clipboard` (approval-gated)                                                                                                                                          |
| `clipboard_write`        | `computer_write_clipboard` / `computer_paste` (approval-gated)                                                                                                                      |
| `launch_app`             | `computer_launch_app` (approval-gated)                                                                                                                                              |
| `bring_to_front`         | `computer_activate_window` (approval-gated, foreground-only, restores prior frontmost)                                                                                              |
| `set_window_frame`       | `computer_set_window_frame` (approval-gated, independent read-back)                                                                                                                 |
| `invoke_menu`            | `computer_invoke_menu` (approval-gated, AX path refuses disabled/absent items)                                                                                                      |
| `kill_app`               | `computer_kill_app` (approval-gated, independent `list_apps` read-back)                                                                                                             |

24 driver tools back the 31 `computer_*` definitions (`computer_wait`,
`computer_run`, `computer_paste`, `computer_perform_action`, and the
double/triple/right-click spellings are Synara-side compositions of the same
tools).

### Surfaced through another driver tool — no separate allowlist entry

| Driver tool    | Reachable as                                                                                                                                                |
| -------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `double_click` | `click` with `count:2` — identical pixel path. Its element-token `AXOpen` recipe is the tracked "secondary actions beyond AXPress" gap, not a missing wire. |
| `right_click`  | `click` with `button:"right"` — identical pixel path; the AX path maps to the same `AXShowMenu` gap.                                                        |

### Allowlisted, internal reads — never agent-facing

The host allowlist gates the server→host `call` method; these names are in it
because `CuaComputerBackend` itself calls them, not because a `computer_*`
tool exposes them.

| Driver tool              | Internal use                                                                                                                                                                                                                         |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `check_permissions`      | `refresh()`/`missingPermissions()` probes; the desktop host intercepts it for the AppSnap helper. Grants surface to the user via the setup card, to the model via result setup notes — a model-facing permission query adds nothing. |
| `check_input_ready`      | `refreshInputPause` resume probe after an `inputPause`. Readiness is reported by dispatch itself; a pre-flight poll cannot tell the model anything the action verdict does not.                                                      |
| `get_agent_cursor_state` | Allowlist headroom for the host-managed agent cursor; no current caller.                                                                                                                                                             |

### Host-internal by decision (workstream-f / capability audit)

| Driver tool                                                | Reason                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ---------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `start_session`, `end_session`, `escalate_session`         | Native session lifecycle is host machinery: the host calls `start_session` at generation bootstrap and owns teardown; escalation is Synara's approval gate's job, not the driver's.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `get_session`, `list_sessions`, `get_session_state`        | Session inventory is host state; provider threads must not see or outlive native sessions.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `set_agent_cursor_enabled`, `set_agent_cursor_theme`       | Cursor ownership: spawn flags (`--compact-cursor --idle-hide-ms 900`) and `set_agent_cursor_motion` at bootstrap set the whole posture.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `set_agent_cursor_motion`                                  | Host calls it once per generation at bootstrap; agent control of overlay physics bypasses the preview contract.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `get_config`, `set_config`                                 | Driver configuration is Synara policy — a session-scoped `max_image_dimension` change would silently break the screenshot-frame mapping every pointer tool depends on.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `start_recording`, `stop_recording`, `get_recording_state` | Recording stays internal until a privacy policy exists (gap 14 / workstream-f).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `replay_trajectory`, `install_ffmpeg`                      | Replay would bypass per-action approval and dispatch contracts; the ffmpeg install belongs to the recording pipeline's owner, not the model.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `check_for_update`                                         | The app owns upgrade lifecycle; the embedded daemon spawns with `CUA_DRIVER_RS_UPDATE_CHECK=0`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `health_report`                                            | Consumer-diagnostics contract ("consumers stay thin"), and every check it runs has a cheaper Synara-native equivalent already wired: `binary_version` = spawn handshake pin, `session_active` = generation liveness/`health().status`, `bundle_identity` = host bundle plumbing, `tcc_*` = `check_permissions` → `missingPermissions`/setup card, `ax_capability` = proven by live AX reads, `screen_capture_capability` = `health().captureAvailable`. A model cannot act on driver internals differently than on the structured errors and setup cards it already gets, so this stays out of the agent surface; revisit only if a user-facing diagnostics card wants the driver's own verdict. |
| `history_status`, `history_query`                          | Privacy policy prerequisite (gap 14); upstream only registers them under the preview admission the embedded host never grants.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |

### Browser surface — owned by the browser workstream

`page` (the legacy compatibility tool: `get_text`/`query_dom`/`execute_javascript`/et al.) and `get_browser_state`, `browser_prepare`, `browser_navigate`, `browser_click`, `browser_type`, `browser_dialog`, `browser_set_input_files`, `browser_download`, `browser_pointer` are a separate feature family with their own consent model. The desktop host refuses them at the allowlist (pinned by `cuaDriverHost.test.ts`); `page`'s generic text extraction overlaps `computer_get_state` `include_text`, so nothing is lost by deferring it.

### Not registered on macOS at this pin

`type_text_chars` (deprecated invoke-time alias of `type_text`, hidden from `tools/list`), `mouse_button_down`, `mouse_button_up`, `mouse_drag`, `parallel_mouse_drag`, and `debug_window_info` exist only in the shared contract vocabulary or other platforms' registries — there is nothing to wire.

## Evidence

- Tool surface: `apps/server/src/agentGateway/computerTools.ts:117` (tool allowlist start), `apps/server/src/agentGateway/computerTools.ts:1906`, `apps/server/src/agentGateway/computerTools.ts:1940`, `apps/server/src/agentGateway/computerTools.ts:2060`, `apps/server/src/agentGateway/computerTools.ts:2152`
- Actions: `apps/server/src/agentGateway/computerTools.ts:2296`, `apps/server/src/agentGateway/computerTools.ts:2338`, `apps/server/src/agentGateway/computerTools.ts:2497`, `apps/server/src/agentGateway/computerTools.ts:2520`, `apps/server/src/agentGateway/computerTools.ts:2579`, `apps/server/src/agentGateway/computerTools.ts:2598`, `apps/server/src/agentGateway/computerTools.ts:2621`, `apps/server/src/agentGateway/computerTools.ts:2644`
- Backend: `apps/server/src/computer/CuaComputerBackend.ts:663`, `apps/server/src/computer/CuaComputerBackend.ts:1146`, `apps/server/src/computer/CuaComputerBackend.ts:1227`, `apps/server/src/computer/CuaComputerBackend.ts:1301`, `apps/server/src/computer/CuaComputerBackend.ts:1331`
- Manager: `apps/server/src/computer/ComputerManager.ts:111`, `apps/server/src/computer/ComputerManager.ts:1508`, `apps/server/src/computer/ComputerManager.ts:1726`
- Host and lifecycle: `apps/desktop/src/cuaDriverHost.ts:610`, `apps/desktop/src/cuaDriverHost.ts:719`, `apps/desktop/src/computerDesktopLifecycle.ts:27`
- Approvals: `apps/server/src/computer/ComputerApprovalGate.ts:18`
- Docs: `docs/computer-use-cua/README.md:56`, `docs/computer-use-cua/capability-audit-2026-09-16.md:9`, `docs/computer-use-cua/capability-audit-2026-09-16.md:24`, `docs/computer-use-cua/evidence/rev15-electron-2026-09-17-notes.md:30`
